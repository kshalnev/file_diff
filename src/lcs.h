#pragma once

#include <vector>

// Returns length of the longest common substring in A and B.
size_t LongestCommonSubstring(std::vector<char> const & a, 
                              std::vector<char> const & b);
